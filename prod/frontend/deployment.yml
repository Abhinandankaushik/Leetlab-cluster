apiVersion: apps/v1
kind: Deployment
metadata:
  name: prod-frontend
  namespace: leetlab
spec:
  replicas: 1
  selector:
    matchLabels:
      app: prod-frontend
  template:
    metadata:
      labels:
        app: prod-frontend
    spec:
      containers:
        - name: frontend
          # The CD workflow rewrites this tag on every deploy.
          image: abhii1716/leetlab-frontend:1b1b0616f6ec71fc3a5a0fe5a16a09bbe175591e
          ports:
            - containerPort: 5173
---
apiVersion: v1
kind: Service
metadata:
  name: prod-frontend
  namespace: leetlab
spec:
  type: LoadBalancer
  selector:
    app: prod-frontend
  ports:
    - port: 80
      targetPort: 5173
