apiVersion: apps/v1
kind: Deployment
metadata:
  name: prod-frontend
  namespace: leetlab
spec:
  replicas: 1
  selector:
    matchLabels:
      app: prod-frontend
  template:
    metadata:
      labels:
        app: prod-frontend
    spec:
      containers:
        - name: frontend
          # The CD workflow rewrites this tag on every deploy.
          image: abhii1716/leetlab-frontend:a09535df9fac8c3e844f2704e5dd67189a891e42
          ports:
            - containerPort: 5173
---
apiVersion: v1
kind: Service
metadata:
  name: prod-frontend
  namespace: leetlab
spec:
  type: LoadBalancer
  selector:
    app: prod-frontend
  ports:
    - port: 80
      targetPort: 5173
