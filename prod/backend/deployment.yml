apiVersion: apps/v1
kind: Deployment
metadata:
  name: prod-backend
  namespace: leetlab
spec:
  replicas: 1
  selector:
    matchLabels:
      app: prod-backend
  template:
    metadata:
      labels:
        app: prod-backend
    spec:
      containers:
        - name: backend
          # The CD workflow rewrites this tag on every deploy.
          image: abhii1716/leetlab-backend:a09535df9fac8c3e844f2704e5dd67189a891e42
          ports:
            - containerPort: 3000
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: leetlab-secrets
                  key: DATABASE_URL
            - name: REDIS_URL
              value: redis://redis:6379
            - name: QUEUE_ENABLED
              value: "true"
            - name: EXECUTION_CONCURRENCY
              value: "5"
            - name: EXECUTOR_LANGUAGES
              value: python,gcc
---
apiVersion: v1
kind: Service
metadata:
  name: prod-backend
  namespace: leetlab
spec:
  selector:
    app: prod-backend
  ports:
    - port: 3000
      targetPort: 3000
